# Configuration cache mandatory from day one (sub-00 §4: a non-compliant
# driver gets isolated into its own included build, never a global disable).
org.gradle.configuration-cache=true
org.gradle.caching=true
org.gradle.parallel=true
