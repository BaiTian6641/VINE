plugins {
    id 'vine.java-shared'
}

// tck-core (sub-21 §2): loader-free TCK machinery — boot runner now, scenario
// DSL / result model / fixture store / differ in Stages B–C. No dependencies:
// the Stage A runner is pure process management; Stage B adds vine-api only,
// and the Prime Invariant holds here too (DSL ports to consumers unchanged).
